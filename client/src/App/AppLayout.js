import styled from 'styled-components';
//styled-components utilises tagged template literals to 
// style components. when defining styles, we create a normal
// React component, that has the styles attached to it.

export default styled.div`
  padding: 40px;   
`
